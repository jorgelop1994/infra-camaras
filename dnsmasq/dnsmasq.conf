# NOTA: En el VPS, dnsmasq usa configs en /etc/dnsmasq.d/
# El archivo relevante es a9-camera.conf
#
# La configuración del sistema está en /etc/dnsmasq.d/a9-camera.conf
# y se cargó automáticamente desde /etc/dnsmasq.conf con conf-dir.
